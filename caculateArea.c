
// Calculate rectangle area
#include <stdio.h>

int main() {
    int length = 5;
    int width = 4;
    int area;

    area = length * width;

    printf("length is:%d\n",length);
    printf("width is:%d\n",width);
    printf("Area is:%d",area);
}